// media/consumers — named constructions → representations (not Orisha).
//
// One-stem companion: this file + consumers.kz. HTML library/item/watch,
// JSON-LD, Link, media/art/subtitle bytes. Reads media/graph; does not
// parse JSON itself. No Koru-level events — dispatch calls pub Zig.
